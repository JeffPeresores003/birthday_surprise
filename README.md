# Photos Directory

Place your photos for the Memory Book in this folder!

Example usage in `src/components/PhotoBook.tsx`:
Set `imageSrc: '/photos/your-photo.jpg'` on any page object inside `MEMORY_PAGES`.
